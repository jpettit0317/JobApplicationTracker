import { SignUpPage } from "../../../components/signup/SignUpPage";

describe("Sign Up Page tests", () => {
    describe("Render tests", () => {
        test("when sign up page has all fields filled in, there should be no errors", () => {
            expect(2 + 2).toBe(4);
        });
    });
});