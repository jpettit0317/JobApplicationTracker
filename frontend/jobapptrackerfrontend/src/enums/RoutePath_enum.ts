export enum RoutePath {
    home = "/",
    login = "/login",
    signup = "/signup",
    jobapplist = "/jobapplist",
    addJobApp = "/addJobApp",
    viewJobApp = "/viewJobApp/",
    viewJobAppPath = "/viewJobApp/:id"
}